# CrownCode — https://hasan-arthur-altuntas.xyz
#
# Crawlers must be able to fetch /_next/static (JS/CSS) to render pages,
# so it is deliberately NOT disallowed. Personal and diagnostic pages are
# excluded with <meta name="robots" content="noindex"> instead of here,
# because a robots.txt block would hide that noindex from crawlers.

User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://hasan-arthur-altuntas.xyz/sitemap.xml
